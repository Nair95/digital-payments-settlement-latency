# Work Log & Resume State (session handoff)

> Purpose: continuity across sessions. Read this first when resuming. Companion docs: `../PROJECT_BLUEPRINT.md` (requirements), `analysis_and_insights.md` + `decision_log.md` (written during notebook phase).

## Approved plan (v2, Group 03)
Deliverables, in order: **(1)** SOLUTION notebook — full analysis with EDA incorporated (`.ipynb` only, no standalone .py anywhere), **(2)** TEMPLATE notebook — same 14-block structure, goals/hints/TODOs only, must run clean end-to-end, **(3)** `docs/analysis_and_insights.md` + `docs/decision_log.md`, **(4)** `dashboard/build_extracts.ipynb` → `dashboard/extracts/*.csv` + `docs/dashboard_spec_powerbi.md` (Power BI, 4 pages), **(5)** executive deck via exec-deck skill, McKinsey style ≤10 slides + PDF export, **(6)** README + requirements.txt, **(7)** git: stage essentials only (no commit). `.gitignore` already updated (agentic artifacts, *.db, *.zip, exploration/, submission/, personal docx).

Naming: `Group_03_Digital_Payments_Settlement_Latency.ipynb` / `..._SOLUTION.ipynb`. Header: Group 03, 7 members placeholder A–G (real names/SAP IDs pending). Both notebooks at repo ROOT (relative db path). Validation: `jupyter nbconvert --to notebook --execute --inplace` (Restart→Run-All equivalent; Anaconda base env has the stack). Template blocks with benchmark assertions (routing lift must land 8.30–8.70pp).

## Decisions so far (to be folded into decision_log.md)
- D8: Dedicated uv environment = the reproducibility story. `pyproject.toml` (pandas/sqlalchemy/matplotlib; dev: ipykernel/nbformat/nbconvert) + `uv.lock` + `.python-version` pinned to uv-managed CPython 3.13.14. `uv sync` rebuilds anywhere with uv. `requirements.txt` = fully pinned export of runtime deps only (`uv export --no-dev --no-hashes --no-annotate`) for evaluators without uv. Notebook kernelspec stays default `python3` (no custom kernel dependency). Batch validation via `uv run jupyter nbconvert --to notebook --execute --inplace`. NOTE: env resolves pandas 3.0.5 — verified numbers below were profiled pre-pandas-3 under Anaconda; recompute in notebook and re-verify against these (never hardcode).
- D1: Group 03 naming; member metadata placeholders.
- D2: EDA lives inside the SOLUTION notebook (user directive; no separate EDA files).
- D3: Settlement-churn analysis scoped to the 2,517 covered merchants (coverage gap); overall covered vs uncovered churn is nearly identical (16.09% vs 16.26%), so coverage does not bias churn selection — state this explicitly.
- D4: "Timeout" counting: by `status='TIMEOUT'` (21,611); note ERR_BANK_TIMEOUT also appears on 17,567 FAILED rows — never mix the two counts.
- D5: SMB breach-exposure buckets: 0%, (0,10%], (10,25%], >25% of batches breaching.
- D6: Annualization for protected volume: trailing 18-month run-rate × 12/18; flat monthly trend observed (breach 27.7–29.5%/mo) supports no-seasonality assumption.
- D7: gpv/MDR from `fact_merchant_settlements` (realized); channel economics from transactions × `mdr_rate_pct` (potential).

## Verified numbers (from read-only profiling — recompute in notebook, do not hardcode)
**Scale**: 530,000 txns (2023-07-01→2024-12-31); 125,000 settlements; 8,500 merchants (SMB 5,455 / MM 2,349 / ENT 696); 12 banks; 16 methods. Integrity clean: 0 orphans, 0 dup PKs/business keys, ledger identities hold; only NULLs = churn_date/churn_reason for 7,122 active merchants.
**Routing A/B**: standard 79.40% (n=264,736) vs dynamic 87.97% (n=265,264) → **+8.57pp lift** (target 8.30–8.70 ✓); latency 1,700→1,035ms (−665ms). Lift by tier and channel ≈ stable (compute exact in notebook).
**Gateway**: PSU avg 1,433ms / p95 6,978 / spikes(≥10s) 1.92% vs T1-PVT 1,322ms / p95 4,096 / 1.47%. Worst banks: BoB 1,442.5ms, Union 1,440.1, PNB 1,440.1 (all PSU); best: IndusInd 1,310.8. Hourly: SR flat 83.3–84.1% BUT **18:00–22:00 avg latency ≈1,530–1,552ms with spike count +55% (≈500–518/hr vs ≈300–350)** — use as the "peak-evening stress" finding instead of success-rate dips.
**Error Pareto** (86,437 failures, 16.31% of attempts): BANK_TIMEOUT 45.33%, AUTH_FAILED 32.91%, INSUFFICIENT_FUNDS 18.26%, NPCI_DEGRADED 1.75%, SWITCH_UNAVAILABLE 1.75%. Upstream infra 48.83% vs user-side 51.17%. USER_DROPPED↔ERR_AUTH_FAILED 1:1 (12,799); ERR_AUTH_FAILED additionally marks 15,646 FAILED rows (total AUTH_FAILED rows = 28,445; the 1:1 pairing claim applies to USER_DROPPED only).
**Settlements**: breach by cycle — T+0 12.22%, T+1 11.96%, T+2 49.56%, T+3 51.74%; delay if breach ≈1.1–1.8 days; delay dist: 0d 89,460 / 1d 20,062 / 2d 10,419 / 3d 4,116 / 4d 943; monthly breach stable ~28%.
**Churn**: overall 16.21% (1,378/8,500); SMB 21.67%, MM 7.41%, ENT 3.16%. Covered merchants (n=2,517): SMB >25% breach → **32.66% churn** vs (0,10%] 10.12% (3.2×); MM >25% → 14.43%. Churn reasons (SMB): SETTLEMENT_DELAY 581 (49% of SMB churn), HIGH_FAILURE_RATE 281, COMPETITOR_SWITCH 227, HIGH_MDR_FEES 93. Churned tenure ≈148d all tiers.
**Economics**: realized MDR ₹2,831,174 on gross ₹217.72M (18 mo; blended 1.30%). Realized by tier: SMB ₹1.8473M/₹142.27M GPV (81,593 batches), MM ₹0.7613M/₹58.38M, ENT ₹0.2226M/₹17.07M (blended ≈1.30% each). Channel potential (SUCCESS txns): UPI GMV ₹400.76M = 55% of SUCCESS GMV at 0% MDR; credit cards ₹147.03M GMV (20%) → ₹2.53M potential (56%); total potential ≈ ₹4.65M. Monthly realized MDR flat ₹0.152–0.165M; **2025-01 stub month ₹0.0084M exists (T+2/T+3 spillover) — trend/annualization window = 2023-07→2024-12 (18 mo)**. At-risk SMB (breach>25%): n=894, churn 292 (32.66%), 18-mo GPV ₹77.43M / MDR ₹1.0028M → annualized ₹51.62M GMV / ₹0.669M MDR at stake; excess-churn model (32.66% − 10.12% = 22.54pp → ~202 merchants) → protected ≈ **₹11.6M GMV / ₹151K MDR per year** (protected share = excess_pp/100 of segment run-rate; assumptions: run-rate annualization, churn reverts to low-breach baseline, retained merchants at segment-average economics, no replacement acquisition credit). CORRECTED: earlier 13.0M/169K estimate was arithmetically wrong (divided excess by churn rate).

## Resume checklist
1. ~~Economics gap profiling~~ DONE (numbers folded into Verified numbers; corrected protected-volume math).
2. ~~Build SOLUTION notebook~~ DONE — `Group_03_Digital_Payments_Settlement_Latency_SOLUTION.ipynb` (37 cells: 23 code / 14 md), built by `exploration/build_solution.py` (scratch, gitignored), executed via `uv run jupyter nbconvert` in the uv env. Zero error outputs; 8 charts; all key numbers re-derived live and match this file; lift +8.57pp PASS (assert in-notebook). Lift stability: by tier 8.21–8.74pp, by channel 8.04–9.18pp. Latency p95 7,336→3,793ms; route×bank clawback ≈666ms avg on worst switches.
3. Execute via nbconvert; verify key numbers vs this file — DONE (see 2).
4. ~~Build TEMPLATE~~ DONE — `Group_03_Digital_Payments_Settlement_Latency.ipynb` (35 cells, goals/hints/TODOs, worked setup + inventory examples, benchmark assert commented); executes error-free via uv env nbconvert. Builder: `exploration/build_template.py`.
5. ~~docs/analysis_and_insights.md + decision_log.md~~ DONE (D1–D8 + conventions + corrections C1–C4; includes protected-volume correction ₹11.6M/₹151K and pairing precision).
6. ~~dashboard/build_extracts.ipynb → 7 CSVs + spec~~ DONE — 7 extracts in `dashboard/extracts/` with SR/breach cross-check assertions (PASS); `docs/dashboard_spec_powerbi.md` (4 pages). Note: nbclient runs notebooks from the notebook's own dir — the extracts notebook locates the DB by walking up from CWD.
7. ~~Executive deck~~ DONE — decksmith pipeline (`E:/LLMs/tools/decksmith/work/group03-capstone/`): 10 slides, SCQA, native charts from verified extracts; judge agent passed all 10 slides after stripping literal `**` markers from bullets. Deliverables: `slides/Group_03_Executive_Deck.pptx` + `.pdf`.
8. ~~README + requirements.txt; git staging~~ DONE — README (repo map, uv+pip run instructions), requirements.txt (pinned export). Git: 18 essentials staged (no commit, per plan); unstaged by choice: 2 course PDFs, main.py placeholder. ZIP assembly (`Group_03_Digital_Payments_Settlement_Latency.zip` with renamed .db) remains for submission time — *.db is gitignored intentionally*.

## Environment notes
- Dedicated uv env (D8): `uv sync` → `.venv` (CPython 3.13.14, uv-managed; NOT Anaconda-based). Run anything in-env via `uv run <cmd>`. requirements.txt regenerated with `uv export --format requirements-txt --no-hashes --no-annotate --no-dev -o requirements.txt`.
- Jupyter MCP server: config enabled in `~/.zcode/cli/config.json`; tools appear in NEW sessions only. Lab server must be running: `E:\LLMs\tools\jupyter-lab-server\start-lab.cmd` (port 8888, token in that README). MCP kernels run on the SERVER's env (not the capstone uv env) — use MCP only for interactive inspection; authoritative execution = `uv run jupyter nbconvert` so the delivered notebook is executed by the declared env.
- Batch validation works without MCP: `uv run jupyter nbconvert --to notebook --execute --inplace <nb>`.
- Windows gotcha: stale `.venv\Scripts\python.exe` image locks can block `uv sync` interpreter swaps; workaround = `mv .venv .venv_stale && uv sync && rm -rf .venv_stale`.
- zg index: workspace indexed (local potion embeddings); .db excluded; blueprint indexed.
